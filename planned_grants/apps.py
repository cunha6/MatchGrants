from django.apps import AppConfig


class PlannedGrantsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "planned_grants"
    verbose_name = "Plano Anual de Avisos"
